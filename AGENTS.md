# Working in this collection

Treat each experiment as a standalone project. Read its README and AGENTS.md before editing. Do not require sibling directories, machine-specific paths or a root-level virtual environment. Scope edits and verification to the selected experiment. Keep the root experiment table current when adding a folder. Do not upload, deploy or publish merely because files are prepared for sharing. Never copy private source repositories wholesale into this collection.
